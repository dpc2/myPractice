#------------------------------------------------#
#         2.6 - The Directory Hierarchy          #
#------------------------------------------------#

ls /

# /unix folder no longer relevant?
# At boot: /boot is read in, followed by /unix

man boot

# File system hierarchy:
# /bin - (binaries) Basic programs like who and ed live here
# /dev - devices
# /etc - (et cetera) Contains various admin files and sytem files
# /lib - (library) Contains primarily parts of the C compiler,
#	preprocessor, subroutine library
# /tmp - (temporaries) Contains short-lived files
# /usr - (user file system) Has its own bin, lib, sbin, src directories
# /usr/man is now in /usr/share/man

